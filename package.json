{
  "name": "namaste-react1",
  "version": "1.0.0",
  "description": "git init\r npm-npm is a package manager we can use in project\r -D-development process\r npm init-package.json\r npm i -D parcel-",
  "scripts": {
    "start":"parcel index.html",
    "build":"parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}

